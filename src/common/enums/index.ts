/**
 * Domain enums are defined in `prisma/schema.prisma` and generated into `@prisma/client`.
 * Re-exported here for a stable import path aligned with the project layout.
 */
export { Country, OrderStatus, Role } from '@prisma/client';
